# Backend API
NEXT_PUBLIC_API_URL=https://apparent-jordanna-pixelart002-42e39ac6.koyeb.app

# Stripe (public key — safe to expose)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe (secret — server only, never expose to browser)
STRIPE_SECRET_KEY=sk_test_...

# Push notifications
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
VAPID_EMAIL=mailto:admin@luviio.in

# Vercel Edge Config (feature flags)
EDGE_CONFIG=your_edge_config_connection_string

# Cookie secret (sign cookies)
COOKIE_SECRET=your-random-32-char-secret-here

# App
NEXT_PUBLIC_APP_NAME=MyStore
NEXT_PUBLIC_APP_URL=https://yourstore.vercel.app
