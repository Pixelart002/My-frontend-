import { useCallback, useEffect, useState } from 'react';
import ConfirmDialog from '../../components/ui/ConfirmDialog';
import { RiAddLine, RiDeleteBinLine, RiEditLine, RiRefreshLine } from '@remixicon/react';
import { adminService, itemsOfList } from '../../services/admin';
import { formatMoney } from '../../utils/format';

const emptyForm={code:'',type:'percent',value:'',min_order_amount:'0',max_discount:'',valid_from:'',valid_until:'',usage_limit:'',per_user_limit:'1',is_active:true,description:''};
function toPayload(form,editing){const payload={type:form.type,value:Number(form.value),min_order_amount:Number(form.min_order_amount||0),per_user_limit:Number(form.per_user_limit||1),is_active:Boolean(form.is_active),description:form.description.trim()};if(!editing)payload.code=form.code.trim().toUpperCase();if(form.max_discount!=='')payload.max_discount=Number(form.max_discount);if(form.valid_from)payload.valid_from=new Date(form.valid_from).toISOString();if(form.valid_until)payload.valid_until=new Date(form.valid_until).toISOString();if(form.usage_limit!=='')payload.usage_limit=Number(form.usage_limit);return payload;}

export default function CouponsPanel({autoOpenCreate=false,capabilities={}}){
 const canCreate=capabilities.couponCreate===true;const canUpdate=capabilities.couponUpdate===true;const canDelete=capabilities.couponDelete===true;
 const [coupons,setCoupons]=useState([]);const [deleteTarget,setDeleteTarget]=useState(null);const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [error,setError]=useState('');const [editing,setEditing]=useState(null);const [formOpen,setFormOpen]=useState(autoOpenCreate&&canCreate);const [form,setForm]=useState(emptyForm);
 const load=useCallback(async()=>{setLoading(true);setError('');try{const result=await adminService.listCoupons({page:1,page_size:100});setCoupons(itemsOfList(result));}catch(err){setError(err?.message||'Unable to load coupons.')}finally{setLoading(false)}},[]);useEffect(()=>{load()},[load]);
 const set=(key)=>(event)=>setForm(current=>({...current,[key]:event.target.type==='checkbox'?event.target.checked:event.target.value}));const openCreate=()=>{if(!canCreate)return;setEditing(null);setForm(emptyForm);setFormOpen(true)};const openEdit=(coupon)=>{if(!canUpdate)return;setEditing(coupon);setForm({...emptyForm,...coupon,value:String(coupon.value),min_order_amount:String(coupon.min_order_amount??0),max_discount:coupon.max_discount==null?'':String(coupon.max_discount),usage_limit:coupon.usage_limit==null?'':String(coupon.usage_limit),per_user_limit:String(coupon.per_user_limit??1),valid_from:coupon.valid_from?String(coupon.valid_from).slice(0,16):'',valid_until:coupon.valid_until?String(coupon.valid_until).slice(0,16):''});setFormOpen(true)};
 const save=async(event)=>{event.preventDefault();if(editing&&!canUpdate)return;if(!editing&&!canCreate)return;setSaving(true);setError('');try{if(!form.code.trim()&&!editing)throw new Error('Coupon code is required.');const value=Number(form.value);if(!Number.isFinite(value)||value<=0)throw new Error('Coupon value must be greater than 0.');if(form.type==='percent'&&value>100)throw new Error('Percent coupon value cannot exceed 100%.');const minOrder=Number(form.min_order_amount||0);if(!Number.isFinite(minOrder)||minOrder<0)throw new Error('Minimum order cannot be negative.');const perUser=Number(form.per_user_limit||1);if(!Number.isInteger(perUser)||perUser<1)throw new Error('Per-user limit must be at least 1.');if(form.usage_limit!==''&&(!Number.isInteger(Number(form.usage_limit))||Number(form.usage_limit)<1))throw new Error('Usage limit must be at least 1 when set.');if(form.max_discount!==''&&Number(form.max_discount)<=0)throw new Error('Max discount must be greater than 0.');if(form.valid_from&&form.valid_until&&new Date(form.valid_until)<=new Date(form.valid_from))throw new Error('Valid until must be later than valid from.');const payload=toPayload(form,editing);if(editing)await adminService.updateCoupon(editing.id,payload);else await adminService.createCoupon(payload);setEditing(null);setForm(emptyForm);setFormOpen(false);await load()}catch(err){setError(err?.message||'Unable to save coupon.')}finally{setSaving(false)}};
 const remove=async(coupon)=>{if(!canDelete||!coupon)return;setDeleteTarget(coupon);};
 return (<><div className="admin-panel"><div className="admin-toolbar"><div><h2>Coupons</h2><p>Customer promo codes with server-side validation, per-user limits and atomic checkout reservation.</p></div><div className="btn-row"><button className="btn btn-quiet btn-sm" type="button" onClick={load} disabled={loading}><RiRefreshLine size={15}/> Refresh</button>{canCreate&&<button className="btn btn-sm" type="button" onClick={openCreate}><RiAddLine size={15}/> New coupon</button>}</div></div>{error&&<div className="form-error" role="alert">{error}</div>}{formOpen&&(canCreate||canUpdate)&&<form className="admin-card" onSubmit={save}><div className="field-grid"><div className="field"><label>Code *</label><input value={form.code} onChange={set('code')} maxLength={50} disabled={Boolean(editing)} placeholder="e.g. WELCOME10"/></div><div className="field"><label>Type</label><select value={form.type} onChange={set('type')}><option value="percent">Percent</option><option value="fixed">Fixed INR</option></select></div></div><div className="field-grid"><div className="field"><label>Value *</label><input type="number" min="0.01" step="0.01" max={form.type==='percent'?'100':undefined} value={form.value} onChange={set('value')}/></div><div className="field"><label>Minimum order</label><input type="number" min="0" step="0.01" value={form.min_order_amount} onChange={set('min_order_amount')}/></div><div className="field"><label>Max discount</label><input type="number" min="0.01" step="0.01" value={form.max_discount} onChange={set('max_discount')} placeholder="No cap"/></div></div><div className="field-grid"><div className="field"><label>Total usage limit</label><input type="number" min="1" step="1" value={form.usage_limit} onChange={set('usage_limit')} placeholder="Unlimited"/></div><div className="field"><label>Per-user limit</label><input type="number" min="1" step="1" value={form.per_user_limit} onChange={set('per_user_limit')}/><small>How many times one customer can redeem this coupon.</small></div><div className="field"><label>Active</label><label className="checkbox-field"><input type="checkbox" checked={form.is_active} onChange={set('is_active')}/> Enabled</label></div></div><div className="field-grid"><div className="field"><label>Valid from</label><input type="datetime-local" value={form.valid_from} onChange={set('valid_from')}/></div><div className="field"><label>Valid until</label><input type="datetime-local" value={form.valid_until} onChange={set('valid_until')}/></div></div><div className="field"><label>Description</label><textarea value={form.description} onChange={set('description')} rows={2}/></div><div className="btn-row"><button className="btn" disabled={saving}>{saving?'Saving…':editing?'Update coupon':'Create coupon'}</button><button className="btn btn-quiet" type="button" onClick={()=>{setEditing(null);setForm(emptyForm);setFormOpen(false)}}>Cancel</button></div></form>}{!canCreate&&!canUpdate&&!canDelete&&<div className="admin-page-note">Read-only access: coupon management actions are restricted to authorised roles.</div>}<div className="admin-card" style={{overflowX:'auto'}}>{loading?<div className="state">Loading coupons…</div>:coupons.length===0?<div className="state">No coupons yet.</div>:<table className="admin-table"><thead><tr><th>Code</th><th>Discount</th><th>Minimum</th><th>Total usage</th><th>Per user</th><th>Status</th><th>Validity</th>{(canUpdate||canDelete)&&<th/>}</tr></thead><tbody>{coupons.map(coupon=><tr key={coupon.id}><td><strong>{coupon.code}</strong><div className="table-sub">{coupon.description||'—'}</div></td><td>{coupon.type==='percent'?`${coupon.value}%`:formatMoney(coupon.value)}{coupon.max_discount!=null&&<div className="table-sub">cap {formatMoney(coupon.max_discount)}</div>}</td><td>{formatMoney(coupon.min_order_amount)}</td><td>{coupon.used_count}{coupon.usage_limit!=null?` / ${coupon.usage_limit}`:' / ∞'}</td><td>{coupon.per_user_limit ?? 1}×</td><td>{coupon.is_active?'Active':'Inactive'}</td><td><div className="table-sub">{coupon.valid_from?new Date(coupon.valid_from).toLocaleString():'Now'}</div><div className="table-sub">{coupon.valid_until?new Date(coupon.valid_until).toLocaleString():'No expiry'}</div></td>{(canUpdate||canDelete)&&<td><div className="btn-row">{canUpdate&&<button className="icon-btn" type="button" title="Edit" onClick={()=>openEdit(coupon)}><RiEditLine size={16}/></button>}{canDelete&&<button className="icon-btn" type="button" title="Delete" onClick={()=>remove(coupon)}><RiDeleteBinLine size={16}/></button>}</div></td>}</tr>)}</tbody></table>}</div></div>
      <ConfirmDialog open={Boolean(deleteTarget)} title="Delete coupon?" message={deleteTarget ? `Delete coupon ${deleteTarget.code}?` : ''} confirmLabel="Delete coupon" danger onCancel={()=>setDeleteTarget(null)} onConfirm={async()=>{const target=deleteTarget;if(!target)return;setDeleteTarget(null);await adminService.deleteCoupon(target.id);await load()}} />
    </>
  );
}
